# Supernote OCR Enhancer - Native launchd Configuration
#
# Copy this file to ~/.supernote-ocr/.env and customize for your setup.
# This file is loaded by the launchd runner script.
#
# PRIVACY NOTE: This file contains paths specific to your system.
# Keep your actual .env file outside the git repo (in ~/.supernote-ocr/).

# =============================================================================
# REQUIRED: Path to your Supernote data directory
# =============================================================================
# This is where your .note files are stored.
# Examples:
#   - Personal Cloud: /Volumes/Storage/Supernote/data
#   - Mac App: ~/Library/Containers/com.ratta.supernote/.../Supernote
#   - Manual: wherever you copy files to
SUPERNOTE_DATA_PATH=/path/to/your/supernote/data

# =============================================================================
# STORAGE MODE - How you sync files with your Supernote device
# =============================================================================
# Options:
#   personal_cloud - Self-hosted Supernote sync server (requires MariaDB)
#   mac_app        - Official Supernote Mac app
#   (blank)        - Manual file transfer (USB, file manager)
STORAGE_MODE=

# -----------------------------------------------------------------------------
# Personal Cloud Mode Settings (if STORAGE_MODE=personal_cloud)
# -----------------------------------------------------------------------------
# Password for MariaDB in the sync server container
# Get it with: docker exec supernote-mariadb env | grep MYSQL_PASSWORD
# MYSQL_PASSWORD=your_password_here

# Path to the sync server's docker-compose.yml (optional, for reference)
# SYNC_SERVER_COMPOSE=/path/to/supernote-sync/docker-compose.yml

# Path to the sync server's .env file (optional, for reference)
# SYNC_SERVER_ENV=/path/to/supernote-sync/.env

# -----------------------------------------------------------------------------
# Mac App Mode Settings (if STORAGE_MODE=mac_app)
# -----------------------------------------------------------------------------
# Usually auto-detected. Only set if auto-detection fails.
# MACAPP_NOTES_PATH=~/Library/Containers/com.ratta.supernote/.../Supernote
# MACAPP_DATABASE_PATH=~/Library/Containers/com.ratta.supernote/.../supernote.db

# =============================================================================
# PROCESSING SETTINGS
# =============================================================================
# Processing interval: 0 = single run (recommended for launchd)
PROCESS_INTERVAL=0

# Log level: DEBUG, INFO, WARNING, ERROR
LOG_LEVEL=INFO

# Write OCR data back to .note files (true/false)
WRITE_TO_NOTE=true

# Create backups before modifying files (true/false)
CREATE_BACKUPS=true

# FILE_RECOGN_TYPE controls Supernote device behavior:
#   0    = Disable device realtime OCR (our OCR only)
#   1    = Enable device realtime OCR (device + our OCR)
#   keep = Preserve file's existing setting
FILE_RECOGN_TYPE=0

# OCR PDF layers: Extract and OCR embedded images from PDF imports (true/false)
OCR_PDF_LAYERS=true

# =============================================================================
# PATHS (usually auto-configured, override if needed)
# =============================================================================
# OCR API endpoint - should be localhost for native execution
# OCR_API_URL=http://localhost:8100

# Data directory for state database and backups
# Defaults to ./data relative to the repo if not set
# DATA_PATH=~/.supernote-ocr/data
