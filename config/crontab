# Set PATH and SHELL for cron jobs
PATH=/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin
SHELL=/bin/bash

# Hourly OCR processing - runs at the top of every hour
0 * * * * /app/scripts/cron-ocr-job.sh

# Ensure newline at end
